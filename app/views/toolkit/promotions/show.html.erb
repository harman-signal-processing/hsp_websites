<%= content_for :breadcrumbs do %>
  <li><%= link_to @brand.name, [:toolkit, @brand] %></li>
  <li><%= link_to "#{@brand.name} Promotions", toolkit_brand_promotions_path(@brand) %></li>
  <li class="current"><%= link_to @promotion.name %></li>
<% end %>

<%= render "toolkit/brands/left_nav" %>

<%= content_for :title do %><%= @brand.name %> <%= @promotion.name %><% end %>

<pre><%= @promotion.to_yaml %></pre>