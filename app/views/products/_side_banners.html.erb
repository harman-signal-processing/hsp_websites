<% ["enterprise", "entertainment"].each do |e| %>
  <% if !!@product.send(e) %>
    <%= link_to "#{ENV['PRO_SITE_URL']}/applications/#{e}", target: "_blank" do %>
      <%= image_tag side_banner_image(e), alt: "#{e.titleize} Solutions" %>
    <% end %>
  <% end %>
<% end %>


