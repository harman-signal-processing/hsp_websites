<%#

I really hate these one-off pages. When it comes time to delete this page--which was
put into place to introduce the freezewah e-pedal, do this:

* Delete this layout page
* Delete app/assets/stylesheets/freezewah.css.scss.erb
* Delete the folder app/assets/images/digitech/freezewah
* Remove 'freezewah.css' from the asset preloading file in
   config/environments/production.rb
   config/environments/staging.rb
* Remove the ProductIntroduction entry for freezewah.

Someday figure out a better way to do all of this.

%>


<% @istomp = Product.find("istomp")  %>
<% set_meta_tags title: "Introducing #{@product.name}",
				 reverse: true %>


<%= content_for :page_css do %>
	<%= stylesheet_link_tag 'application', website.folder, 'istomp', 'freezewah', cache: false, media: :all %>
	<%= javascript_include_tag 'istomp' %>
	<style type="text/css">
	<%= raw @product_introduction.extra_css.to_s %>
	</style>
<% end %>

<%= content_for :alternate_logo do %>
	<%= link_to(image_tag("#{website.folder}/logo_black.png", alt: Setting.site_name(website)), main_app.locale_root_path) %>
<% end %>

<%= content_for :footer_logo do %>
	<%= image_tag "harman.jpg" %>
<% end %>

<div id="introducing_freezewah">
	<h1><%= link_to(image_tag('digitech/freezewah/top.png', alt: "Introducing Freeze Wah for iStomp"), @product) %></h1>

	<div class="middle_content_container">
		<section id="istomp_content">
			<div style="float: right; margin: 4px 4px 8px 8px">
				<%= link_to(image_tag(@istomp.photo.product_attachment.url(:epedal)), @istomp) %>
			</div>
			<h2><%= link_to(image_tag('digitech/istomp/made_for_istomp.png', alt: "Made for iStomp"), @istomp) %></h2>
			<h5>The Pedal That Changes Everything.</h5>
			<div class="text_content">
				<%=raw truncate strip_html(@istomp.description), length: 340 %>
				<%= link_to "more", @istomp %>
			</div>
			<%= image_tag("made_for_ipod_ipad_iphone.png", alt: "Made for iPod, iPad, iPhone") %>
		</section>	
		<section id="product_content">
			<div style="float: left; margin: 4px 8px 8px 4px;"><%= link_to(image_tag('digitech/freezewah/pedal.png', alt: "Freeze Wah Pedal"), @product) %>
			</div>
			<h2><%= @product.name %></h2>
			<h4>Wah Pedal</h4>
			<div class="text_content" style="width: 475px">
				<%=raw truncate strip_html(@product.description), length: 540, omission: "" %>
			</div>
			<h5>Regular Price: <%= number_to_currency(@product.msrp.to_f) %></h5>
		</section>

	</div>
	<section id="bottom">
		<h3>Browse all <%= @istomp.sub_products.count %> e-pedals</h3>
		<%= link_to(image_tag('digitech/istomp/e-pedals_lineup.png', alt: "All e-pedals"), url_for(@istomp.softwares.first)) %>
	</section>
	<div class="hint" style="text-align: center; margin-top: 5px;">
		* Introductory price valid for 30 days after launch.
	</div>
</div>

