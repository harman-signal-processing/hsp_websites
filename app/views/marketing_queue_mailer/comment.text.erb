Just posted on the marketing queue for the <%= @marketing_comment.project_or_task.brand.name %>
<%= @marketing_comment.project_or_task.is_a?(MarketingProject) ? "project" : "task" %>
<%= @marketing_comment.project_or_task.name %>:

From: <%= @marketing_comment.user.name %>

<%= @marketing_comment.message %>
