Hi. Here's what's happening on the Marketing Queue:
http://marketingqueue.harmanpro.com

<% if @user.role?(:queue_admin) && @unassigned_tasks.count > 0 %>
Unassigned tasks:
----------------------------------------------
<% @unassigned_tasks.each do |marketing_task| %>
 * <%= marketing_task.name %> (<%= marketing_task.brand.name %> <% if marketing_task.marketing_project %><%= marketing_task.marketing_project.name %><% end %>)
     due: <%= I18n.l marketing_task.due_on, format: :long %>
<% end %>
<% end %>

<% if @my_tasks.count > 0 %>
Here are tasks assigned to you:
----------------------------------------------
<% @my_tasks.each do |marketing_task| %>
 [ ] <%= marketing_task.name %> (<%= marketing_task.brand.name %> <% if marketing_task.marketing_project %><%= marketing_task.marketing_project.name %><% end %>)
     due: <%= I18n.l marketing_task.due_on, format: :long %>

<% end %>
<% end %>

<% if @my_projects.count > 0 %>
Progress on your projects:
----------------------------------------------
<% @my_projects.each do |marketing_project| %>
 * <%= marketing_project.name %> (<%= number_to_percentage marketing_project.percent_complete, precision: 0 %> complete)

<% end %>
<% end %>

<% if @new_projects.count > 0 %>
The latest new projects:
----------------------------------------------
<% @new_projects.each do |marketing_project| %>
 * <%= "#{marketing_project.brand.name}: #{marketing_project.name}" %> (<%= marketing_project.marketing_tasks.count %> tasks)
	 due: <%= I18n.l marketing_project.due_on, format: :long %>
<% end %>
<% end %>

<% if @open_projects.length > 0 %>
ALL open projects:
----------------------------------------------
<% @open_projects.each do |marketing_project| %>
 * <%= "#{marketing_project.brand.name}: #{marketing_project.name}" %> (<%= number_to_percentage marketing_project.percent_complete, precision: 0 %> complete)
<% end %>
<% end %>
