<h1>Edit Project</h1>

<div class="row">
	<div class="large-8 columns">
		<%= render 'form' %>
	</div>
	<div class="large-4 columns">
		<div class="tip">
			<p>That fancy checkbox under the due date
				can be confusing. Here's what it means:</p>
			<p><%= check_box_tag 'foo1', 1, true, disabled: true, class: "boolean optional" %> CHECKED<br/>
				If you check the box and move the <b>project</b> due date forward
				by one month, then the due dates for all the <b>open tasks</b>
				for this project will also move forward by one month.</p>
			<p><%= check_box_tag 'foo2', 0, false, disabled: true, class: "boolean optional" %> UNCHECKED<br/>
				If you un-check the box and move the 
				<b>project</b> due date by one month, then the
				<b>open tasks</b> will not change.</p>
		</div>
	</div>
</div>

<% if @marketing_project.incomplete_marketing_tasks.count > 0 %>
<div class="row">
	<div class="large-12 columns">
		<h4 class="subheader">If you really just wanted to edit one of the project's tasks,
			click on the task name below:</h4>

		<ul class="list">
			<% @marketing_project.incomplete_marketing_tasks.each do |marketing_task| %>
			<li> <%= link_to marketing_task.name, edit_marketing_queue_marketing_task_path(marketing_task) %>
				<i>
					<% if marketing_task.worker %>
					assigned to: <%= marketing_task.worker.name %>,
					<% end %>
					due: <%=l marketing_task.due_on, format: :short %>
				</i>
			</li>
			<% end %>
		</ul>
	</div>
</div>
<% end %>
