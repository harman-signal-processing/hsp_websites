<h1>New software</h1>

<%= form_for([:admin, @software], :html => {:multipart => true}) do |f| %>
  <%= render "form", :f => f %>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', admin_softwares_path %>