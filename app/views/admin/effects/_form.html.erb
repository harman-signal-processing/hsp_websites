<%= simple_form_for([:admin, @effect], html: {multipart: true}) do |f| %>
  <%= f.association :effect_type, collection: EffectType.order(:position) %>
  <%= f.input :name %>
  <br/><%= f.button :submit %>
<% end %>
