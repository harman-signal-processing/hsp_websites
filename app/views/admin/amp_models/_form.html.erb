<%= form_for([:admin, @amp_model], html: {multipart: true}) do |f| %>
  <% if @amp_model.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@amp_model.errors.count, "error") %> prohibited this amp model from being saved:</h2>

      <ul>
      <% @amp_model.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
