<h1>New artist</h1>

<%= form_for([:admin, @artist], html: {multipart: true}) do |f| %>
  <%= render "form", f: f %>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', admin_artists_path %>