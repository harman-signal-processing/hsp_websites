<%= simple_form_for([:admin, @amx_dxlink_attribute_name]) do |f| %>
  <%= f.input :name, label: "Name" %>

    </br>
  <%= f.button :submit %>
<% end # simple_form_for([:admin, @amx_dxlink_attribute_name]) do |f|%>
