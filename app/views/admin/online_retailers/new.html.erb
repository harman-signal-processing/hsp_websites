<h1>New Online Retailer</h1>

<%= form_for([:admin, @online_retailer], :html => {:multipart => true}) do |f| %>
  <%= render "form", :f => f %>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', admin_online_retailers_path %>