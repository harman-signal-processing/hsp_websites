<%= simple_form_for([:admin, @vip_email]) do |f| %>
  <%= f.input :label, label: "Label" %>
  <%= f.input :email, label: "Email" %>

    </br>
  <%= f.button :submit %>
<% end # simple_form_for([:admin, @vip_email]) do |f|%>