<li id="product_stompbox_model_<%= psm.id %>">
	<%= psm.stompbox_model.name %>
	<%= link_to(image_tag('icons/trash.gif'),
		  admin_product_stompbox_model_path(psm),
		  remote: true,
		  confirm: "Are you sure you want to delete this amp model?",
		  method: :delete) %>
</li>