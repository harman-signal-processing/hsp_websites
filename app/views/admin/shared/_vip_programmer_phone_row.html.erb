<% called_from ||= 'vip_programmer' %>
<li id="vip_programmer_phone_<%= association.id %>">
	<% if (called_from == 'vip_programmer') %><span class="handle"><%= image_tag("icons/drag_handle.gif") %></span><% end %>
	<%= (called_from == 'vip_programmer') ? link_to(association.phone.phone, [:admin, association.phone]) : link_to(association.programmer.name, [:admin, association.programmer]) %> 
	<%= link_to(image_tag("icons/trash.gif"), 
			admin_vip_programmer_phone_path(association), 
			confirm: "Are you sure? (This won't delete the phone --it will only remove the association.)", 
			remote: true,
			method: :delete) %>
</li>
