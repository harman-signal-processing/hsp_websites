<%= simple_form_for([:admin, @vip_phone]) do |f| %>
  <%= f.input :label, label: "Label" %>
  <%= f.input :phone, label: "Phone" %>

    </br>
  <%= f.button :submit %>
<% end # simple_form_for([:admin, @vip_phone]) do |f|%>