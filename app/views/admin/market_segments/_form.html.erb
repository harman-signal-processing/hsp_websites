<%= form_for([:admin, @market_segment], html: {multipart: true}) do |f| %>
  <% if @market_segment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@market_segment.errors.count, "error") %> prohibited this market segment from being saved:</h2>

      <ul>
      <% @market_segment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
