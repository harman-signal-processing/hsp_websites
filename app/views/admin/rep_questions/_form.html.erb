<%= form_for([:admin, @rep_question]) do |f| %>
  <% if @rep_question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rep_question.errors.count, "error") %> prohibited this rep question from being saved:</h2>

      <ul>
      <% @rep_question.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :rep_report_id %><br />
    <%= f.text_field :rep_report_id %>
  </div>
  <div class="field">
    <%= f.label :question %><br />
    <%= f.text_area :question %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
