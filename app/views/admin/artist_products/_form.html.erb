<%= simple_form_for([:admin, @artist_product]) do |f| %>
  <%= f.association :artist, collection: Artist.order(:name) %>
  <%= f.association :product, collection: Product.order(:name) %>
	<%= f.input :quote %>
  <br/><%= f.input :on_tour %><br/>
  <br/><%= f.button :submit %>
<% end %>
