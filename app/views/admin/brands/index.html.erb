<h1>Brands</h1>
<% if can? :create, Brand %>
<ul class="button-group round">
	<li><%= link_to 'New Brand', new_admin_brand_path, class: "small success button" %></li>
</ul>
<% end %>

<ul class="list">
<% @brands.each do |brand| %>
  <li><%= link_to brand.name, [:admin, brand] %></li>
<% end %>
</ul>
