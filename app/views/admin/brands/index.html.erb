<h1>Brands</h1>
<% if can? :create, Brand %>
<div class="button_container">
	<%= link_to 'New Brand', new_admin_brand_path, class: "new button" %>
</div>
<% end %>

<ul>
<% @brands.each do |brand| %>
  <li><%= link_to brand.name, [:admin, brand] %></li>
<% end %>
</ul>
