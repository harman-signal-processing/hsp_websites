<h1>New Product Document</h1>

<%= form_for([:admin, @product_document], html: {multipart: true}) do |f| %>
  <%= render "form", f: f %>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', admin_product_documents_path %>