<h1>New Product Family</h1>

<%= form_for [:admin, @product_family], :html => { :multipart => true } do |f| %>
  <%= render "form", :f => f %>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', admin_product_families_path %>