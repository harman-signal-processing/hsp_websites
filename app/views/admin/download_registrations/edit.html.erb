<h1>Editing <%= @download_registration.registered_download.name %> registration</h1>

<%= render 'form' %>

<% if @download_registration.receipt.present? %>
<p>
	<%= link_to "Receipt", @download_registration.receipt.url %>
</p>
<% end %>