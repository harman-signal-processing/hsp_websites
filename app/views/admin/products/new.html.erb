<h1>New product</h1>

<%= form_for([:admin, @product], html: {multipart: true}) do |f| %>
  <%= render "form", f: f %>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', admin_products_path %>